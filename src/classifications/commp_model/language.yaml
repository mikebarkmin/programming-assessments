default: &default
  build_and_run:
  syntax:
  semantic:
  standard_library:

java:
  <<: *default

python:
  <<: *default

pseudocode:
  <<: *default
  _meta:
    description: >
      The guide can be found here: http://tiny.cc/codeguide
